# ---- Secrets (NEVER commit) ----
local.env
.env
*.local.env

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# ---- Raw dataset: 531 MB, postings.csv exceeds GitHub's 100 MB limit ----
# Not needed to run the app (the app loads project/output/gold_linkedin_with_clusters.csv.gz).
# A 1,000-row raw sample is kept in before_after_cleaning/ for the before/after comparison.
# Full raw source: LinkedIn Job Postings dataset (Kaggle).
Job Posting Dataset/

# ---- Full master table: 890 MB, exceeds GitHub's 100 MB limit ----
# Kept locally only. It is the cleaned merge output (Phase 2); the final clustered
# dataset the app actually uses is project/output/gold_linkedin_with_clusters.csv.gz.
project/output/master_jobs_dataset.csv

# ---- OS / editor ----
.DS_Store
Thumbs.db
.idea/
.vscode/
